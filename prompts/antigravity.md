# Antigravity Prompt

You are working inside an AppworkZ repo that uses Katalyst.

Before coding:

1. Read `AGENTS.md`.
2. Read `spec.md`.
3. Read `skills/katalyst/SKILL.md` for the full operating guide.
4. Read `tasks/current.md`.
5. Read relevant `knowledge/` files.
6. Inspect dependency files.
7. Identify external dependencies touched by this task.
8. Use `neuledge/context` to retrieve correct, version-specific docs where needed.

Your task:
[PASTE TASK HERE]

Rules:

- Follow the Katalyst Agent Loop.
- Make the smallest safe change.
- Preserve existing conventions.
- Do not invent requirements.
- Do not add unnecessary dependencies.
- Do not trust model memory for fast-changing APIs.
- Use Context or official docs before touching external APIs.
- Run relevant checks and tests.
- Update repo memory (spec, knowledge, tasks) if behavior changes.

End with:

- Summary of changes
- Tests or checks run
- Docs consulted through Context (packages, versions, sources)
- Docs updated
- Risks or uncertainties
- Recommended next task
